name: Generate family HTML

on:
  workflow_dispatch:
  push:
    paths:
      - 'family.yaml'
      - 'workflows/scripts/generate-html-from-yaml.js'
      - 'family.html'
  pull_request:
    paths:
      - 'family.yaml'
      - 'workflows/scripts/generate-html-from-yaml.js'
      - 'family.html'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Generate HTML from YAML
      run: node workflows/scripts/generate-html-from-yaml.js

    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git diff --quiet && exit 0
        git add faq/*.html
        git add *.html
        git commit -m 'Generate HTML from YAML'
        git pull
        git push
